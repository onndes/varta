// ╔══════════════════════════════════════════════════════════════════════════════╗
// ║   НАЛАШТУВАННЯ ДРУКУ (компонент) — РЕДАГУЙ ЦЕЙ ФАЙЛ                        ║
// ╚══════════════════════════════════════════════════════════════════════════════╝
//
// Зовнішній вигляд (розміри, кольори, поля сторінки) — у файлі:
//   src/styles/_print.scss  →  шукай блок «НАЛАШТУВАННЯ ДРУКУ»

// ── 1. СИМВОЛ НАРЯДУ ──────────────────────────────────────────────────────────
/**
 * Що відображати у закрашеній клітинці наряду.
 *   'Н'     — велика буква (поточне)
 *   '08:00' — час (попереднє)
 *   '●'     — або будь-який інший символ чи текст
 */
export const PRINT_DUTY_MARK = 'Н';

// ── 2. ВІДОБРАЖЕННЯ СТАТУСІВ У КЛІТИНКАХ ──────────────────────────────────────
/**
 * true  → показувати «Відпустка», «Лікарняний», «Відрядження» тощо
 * false → залишати клітинку порожньою (тільки наряди видно)
 */
export const PRINT_SHOW_STATUS_LABELS = true;

// ── 3. ГЛИБИНА СТАТИСТИКИ «ТИЖНІВ ТОМУ» ──────────────────────────────────────
/**
 * Скільки тижнів назад перевіряти той самий день тижня.
 * Наприклад, 4 → покаже «1», «2», «3» або «4» тижні тому.
 * Увімкнути/вимкнути статистику загалом — через вибір режиму друку
 * «Графік (таблиця + статистика)» або «Графік (таблиця)».
 */
export const PRINT_DOW_HISTORY_WEEKS = 4;
